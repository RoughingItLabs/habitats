/**
 * EmberWolf.tsx
 * SVG sprite — 5 stages.
 * TODO: Replace with actual SVG art assets.
 */
import React from 'react';
import { CreatureStage } from '../../../types/creature';

interface EmberWolfProps {
  stage: CreatureStage;
  size: number;
}

export function EmberWolf({ stage, size }: EmberWolfProps) {
  return null;
}
