/**
 * ForestDeer.tsx
 * SVG sprite — 5 stages.
 * TODO: Replace with actual SVG art assets.
 */
import React from 'react';
import { CreatureStage } from '../../../types/creature';

interface ForestDeerProps {
  stage: CreatureStage;
  size: number;
}

export function ForestDeer({ stage, size }: ForestDeerProps) {
  return null;
}
