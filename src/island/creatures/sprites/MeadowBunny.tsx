/**
 * MeadowBunny.tsx
 * SVG sprite — 5 stages.
 * TODO: Replace with actual SVG art assets.
 */
import React from 'react';
import { CreatureStage } from '../../../types/creature';

interface MeadowBunnyProps {
  stage: CreatureStage;
  size: number;
}

export function MeadowBunny({ stage, size }: MeadowBunnyProps) {
  return null;
}
