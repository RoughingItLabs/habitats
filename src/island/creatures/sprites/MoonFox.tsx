/**
 * MoonFox.tsx
 * SVG sprite — 5 stages.
 * TODO: Replace with actual SVG art assets.
 */
import React from 'react';
import { CreatureStage } from '../../../types/creature';

interface MoonFoxProps {
  stage: CreatureStage;
  size: number;
}

export function MoonFox({ stage, size }: MoonFoxProps) {
  return null;
}
