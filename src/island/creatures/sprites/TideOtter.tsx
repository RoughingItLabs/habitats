/**
 * TideOtter.tsx
 * SVG sprite — 5 stages.
 * TODO: Replace with actual SVG art assets.
 */
import React from 'react';
import { CreatureStage } from '../../../types/creature';

interface TideOtterProps {
  stage: CreatureStage;
  size: number;
}

export function TideOtter({ stage, size }: TideOtterProps) {
  return null;
}
