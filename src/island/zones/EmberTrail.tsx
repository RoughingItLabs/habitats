/**
 * EmberTrail.tsx
 * TODO (Prompt 4): Implement Skia zone renderer.
 */
import React from 'react';
import { ZoneData } from '../../types/zone';

interface EmberTrailProps {
  zone: ZoneData;
}

export function EmberTrail({ zone }: EmberTrailProps) {
  return null;
}
