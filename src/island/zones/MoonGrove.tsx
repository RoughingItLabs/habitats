/**
 * MoonGrove.tsx
 * TODO (Prompt 4): Implement Skia zone renderer.
 */
import React from 'react';
import { ZoneData } from '../../types/zone';

interface MoonGroveProps {
  zone: ZoneData;
}

export function MoonGrove({ zone }: MoonGroveProps) {
  return null;
}
