/**
 * StonePeak.tsx
 * TODO (Prompt 4): Implement Skia zone renderer.
 */
import React from 'react';
import { ZoneData } from '../../types/zone';

interface StonePeakProps {
  zone: ZoneData;
}

export function StonePeak({ zone }: StonePeakProps) {
  return null;
}
