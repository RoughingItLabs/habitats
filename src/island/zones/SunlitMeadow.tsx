/**
 * SunlitMeadow.tsx
 * TODO (Prompt 4): Implement Skia zone renderer.
 */
import React from 'react';
import { ZoneData } from '../../types/zone';

interface SunlitMeadowProps {
  zone: ZoneData;
}

export function SunlitMeadow({ zone }: SunlitMeadowProps) {
  return null;
}
