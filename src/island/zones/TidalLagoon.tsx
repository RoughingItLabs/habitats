/**
 * TidalLagoon.tsx
 * TODO (Prompt 4): Implement Skia zone renderer.
 */
import React from 'react';
import { ZoneData } from '../../types/zone';

interface TidalLagoonProps {
  zone: ZoneData;
}

export function TidalLagoon({ zone }: TidalLagoonProps) {
  return null;
}
