/**
 * ZenClearing.tsx
 * TODO (Prompt 4): Implement Skia zone renderer.
 */
import React from 'react';
import { ZoneData } from '../../types/zone';

interface ZenClearingProps {
  zone: ZoneData;
}

export function ZenClearing({ zone }: ZenClearingProps) {
  return null;
}
